{
  "name": "fastube-python-backend",
  "version": "1.0.0",
  "description": "YouTube 자막 추출 파이썬 백엔드",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "dev": "uvicorn app.main:app --host 0.0.0.0 --port 4000 --reload"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "private": true
}
